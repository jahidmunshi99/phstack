import TopAction from "@/components/reehabilitation/TopAction";
import TopHeader from "@/components/reehabilitation/TopHeader";

const TopDetailsLayout = () => {
  return (
    <div className="flex items-center justify-between">
      <TopHeader />
      <TopAction />
    </div>
  );
};

export default TopDetailsLayout;
